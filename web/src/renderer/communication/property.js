const CLIENT_ID = 'py_auto_stzb'

const HOST = 'localhost'
const PORT = 33333

const PROTOCOL = 'ws://'

const WEBSOCKET_LINK = PROTOCOL + HOST + ':' + PORT + '/' + CLIENT_ID

export {
    CLIENT_ID,
    HOST,
    PORT,
    WEBSOCKET_LINK
}